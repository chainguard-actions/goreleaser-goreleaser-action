name: Test basic install-only
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        with:
          install-only: 'true'
          version: '~> v2'
      - name: Assert goreleaser is installed
        run: |
          which goreleaser
          goreleaser --version
          echo "PASS: goreleaser is installed"
